# reference: http://www.objc.io/issue-6/travis-ci.html

language: objective-c
install:
  - gem install xcpretty --no-document
  - cd Example && pod install && cd -
script:
- xcodebuild -workspace Example/CloudantQueryObjc.xcworkspace -scheme 'CloudantQueryObjc' -destination 'platform=iOS Simulator,OS=7.1,name=iPhone Retina (3.5-inch)' ONLY_ACTIVE_ARCH=NO build test | xcpretty; exit ${PIPESTATUS[0]}
